# Automatically created by: scrapydd
from setuptools import setup, find_packages
setup(
    name         = 'org_example',
    version      = '1.0',
    packages     = find_packages(),
    entry_points = {'scrapy': ['settings = org_example.settings']},
    install_requires = [],
)
